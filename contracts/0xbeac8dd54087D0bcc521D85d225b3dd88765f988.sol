contract main {




// =====================  Runtime code  =====================


array of address addresses;

function addresses(uint256 arg1) payable {
    require calldata.size - 4 >= 32
    require arg1 < addresses.length
    return addresses[arg1]
}

function _fallback() payable {
    revert
}

function sub_279c0797(?) payable {
    require calldata.size - 4 >= 32
    idx = 0
    while idx <= arg1:
        addresses.length++
        mem[0] = 0
        addresses[addresses.length] = msg.sender
        if idx == -1:
            revert with 0, 17
        idx = idx + 1
        continue 
}



}
