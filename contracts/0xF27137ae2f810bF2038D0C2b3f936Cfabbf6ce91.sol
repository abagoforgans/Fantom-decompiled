contract main {




// =====================  Runtime code  =====================


array of uint256 y;

function y() payable {
    return y[0 len y.length]
}

function _fallback() payable {
    revert
}

function sub_d73084d3(?) payable {
    require calldata.size - 4 >= 32
    require arg1 == arg1
    return arg1
}

function sub_a15920ca(?) payable {
    require calldata.size - 4 >= 32
    require arg1 == uint32(arg1)
    return (arg1 << 224)
}

function sub_2e500071(?) payable {
    require calldata.size - 4 >= 32
    require arg1 == arg1
    mem[128] = arg1
    y.length = 65
    s = 0
    idx = 128
    while 160 > idx:
        y[s] = mem[idx]
        s = s + 1
        idx = idx + 32
        continue 
    idx = 1
    while y.length + 31 / 32 > idx:
        y[idx] = 0
        idx = idx + 1
        continue 
}



}
